import { LeafletMap } from "./components/LeafletMap/LeafletMap";

function App() {
    return (
        <div className="app-container">
            <LeafletMap />
        </div>
    );
}

export default App;
